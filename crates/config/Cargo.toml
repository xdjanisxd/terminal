[package]
name = "terminal-config"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
terminal-workspace = { path = "../workspace" }
serde = { version = "1", features = ["derive"] }
toml = "0.9"
